syntax = "proto3";

package serial;

message ControlPacket {
    uint32 address = 10;
    Command command = 20;
    uint32 payload = 30;
}

enum Command {
    DRIVE = 0;
    PROGRAM = 10;
    RESET = 100;
}
